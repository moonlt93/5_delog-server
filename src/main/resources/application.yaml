spring:
  batch:
    jdbc:
      initialize-schema: never
      table-prefix: BATCH_
  application:
    name: server
  datasource:
    url: jdbc:mysql://localhost:3306/gachi
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
